@model List<ResultCheffDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Şef Listesi</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered text-muted table-responsive-sm">
                    <thead>
                        <tr>
                            <td>#</td>
                            <td>Resim</td>
                            <td>Ad Soyad</td>
                            <td>Ünvan</td>
                            <td class="text-center" colspan="2">İşlemler</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <th>@count</th>
                                <td>@item.ImageURL</td>
                                <td>@item.Name</td>
                                <td>@item.Title</td>
                                <td style="width:100px" class="text-center">
                                    <a href="/Admin/Cheff/DeleteCheff/@item.CheffId" class="btn btn-outline-danger"> <i class="fa fa-trash"></i> Sil</a>

                                </td>
                                <td style="width:130px" class="text-center">
                                    <a href="/Admin/Cheff/UpdateCheff/@item.CheffId" class="btn btn-outline-success"><i class="fa fa-edit"></i> Güncelle</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Admin/Cheff/CreateCheff" class="btn btn-outline-primary"> <i class="fa fa-pencil"></i> Yeni Şef Ekle</a>
            </div>
        </div>
    </div>
</div>






