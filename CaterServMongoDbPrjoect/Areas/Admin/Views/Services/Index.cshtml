@model List<ResultServiceDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
    int count = 0;
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            <h4 class="card-title">Hizmetler Listesi</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered text-muted table-responsive-sm">
                    <thead>
                        <tr>
                            <td>#</td>
                            <td>Başlık</td>
                            <td>Açıklama</td>
                            <td class="text-center" colspan="2">İşlemler</td>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <th>@count</th>
                                <td>@item.Title</td>
                                <td>@item.Descripition</td>
                                <td style="width:100px" class="text-center">
                                    <a href="/Admin/Services/DeleteService/@item.ServiceID" class="btn btn-outline-danger"> <i class="fa fa-trash"></i> Sil</a>

                                </td>
                                <td style="width:130px" class="text-center">
                                    <a href="/Admin/Services/UpdateService/@item.ServiceID" class="btn btn-outline-success"><i class="fa fa-edit"></i> Güncelle</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a href="/Admin/Services/CreateService" class="btn btn-outline-primary"> <i class="fa fa-pencil"></i> Yeni Hizmet Oluştur</a>
            </div>
        </div>
    </div>
</div>




